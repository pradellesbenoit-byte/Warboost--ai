export default function handler(req,res){res.setHeader("Cache-Control","no-store");const url=String(process.env.SUPABASE_URL||"");const key=String(process.env.SUPABASE_ANON_KEY||process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY||"");res.status(200).json({configured:Boolean(url&&key),url:key?url:"",key:key||""})}
